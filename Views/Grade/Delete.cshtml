@model UniversityApp.Models.Grade

@{
    ViewData["Title"] = "Удалить оценку";
}

<h2>Удалить оценку</h2>

<h3>Вы уверены, что хотите удалить эту оценку?</h3>
<div>
    <h4>Студент: @Model.Student?.Name</h4>
    <h4>Дисциплина: @Model.Course?.Title</h4>
    <h4>Оценка: @Model.Score</h4>
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Назад к списку</a>
    </form>
</div>